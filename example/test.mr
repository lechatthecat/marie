// you should use full path instead of relative path
use ./example/test2;

let b = new B();
print(b.name);

fn fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}

let before = clock();
print(fib(15));
let after = clock();
print(after - before << "ms");
